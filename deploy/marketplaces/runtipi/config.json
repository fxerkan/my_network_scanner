{
  "$schema": "../schema.json",
  "name": "MyNeS - My Network Scanner",
  "id": "mynes",
  "available": true,
  "short_desc": "Discover, identify and monitor every device on your LAN - Wi-Fi, Zigbee, Z-Wave, BLE and Matter.",
  "author": "fxerkan",
  "port": 5883,
  "categories": ["network", "utilities"],
  "description": "MyNeS discovers, identifies and monitors every device on your home network, including the ones an IP scan cannot see. It speaks ARP, mDNS/Bonjour, SSDP/UPnP, Matter, Bluetooth LE and MQTT, so Zigbee2MQTT bulbs, Z-Wave JS sensors, BLE trackers and Tasmota nodes show up alongside your laptops and NAS. Vendor lookup over a 1000+ entry OUI database, topology and graph views, scan history, rule-based alerts by Web Push / webhook / e-mail, and two-way Home Assistant integration. No cloud, no account, no telemetry.",
  "tipi_version": 1,
  "version": "1.3.0",
  "source": "https://github.com/fxerkan/my_network_scanner",
  "website": "https://github.com/fxerkan/my_network_scanner",
  "exposable": true,
  "supported_architectures": ["arm64", "amd64"],
  "dynamic_config": false,
  "form_fields": [
    {
      "type": "password",
      "label": "Master password (encrypts stored device credentials, auto-generated if empty)",
      "required": false,
      "env_variable": "MYNES_PASSWORD"
    },
    {
      "type": "text",
      "label": "MQTT broker host (optional - reveals Zigbee/Z-Wave/Tasmota devices)",
      "required": false,
      "env_variable": "MYNES_MQTT_HOST"
    },
    {
      "type": "text",
      "label": "Home Assistant URL (optional)",
      "required": false,
      "env_variable": "MYNES_HA_URL"
    },
    {
      "type": "password",
      "label": "Home Assistant long-lived access token (optional)",
      "required": false,
      "env_variable": "MYNES_HA_TOKEN"
    }
  ]
}
