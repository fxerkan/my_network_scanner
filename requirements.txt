# Dependencies live in pyproject.toml; this file mirrors core + "discovery"
# so `pip install -r requirements.txt` keeps working.
# Preferred install:  pip install -e ".[all]"
flask>=3.0
python-nmap>=0.7
requests>=2.31
scapy>=2.5
mac-vendor-lookup>=0.1.12
dnspython>=2.3
psutil>=5.9
cryptography>=41.0
zeroconf>=0.131
paho-mqtt>=1.6
